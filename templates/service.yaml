apiVersion: v1
kind: Service
metadata:
  name: {{ include "joohan-febe-app.fullname" . }}
  labels:
    {{- include "joohan-febe-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "joohan-febe-app.selectorLabels" . | nindent 4 }}
